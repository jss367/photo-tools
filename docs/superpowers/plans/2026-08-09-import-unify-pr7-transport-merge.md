# Import Unification PR 7: The Transport Merge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Introduce `_LocalTransport`/`_RsyncTransport`, move each path's transport core behind them verbatim, then delete `_run_remote_import_job` — one orchestrator, one import implementation, the project's finale.

**Architecture:** Seven commits per the definitive 2026-08-09 pre-merge map (key content embedded per task): docs (spec amendments recording three deliberate adaptations of the protocol sketch + decision 12) → zero-flip hoists → the `attests_bytes` rename → the decision-12 guard port (red-green, the ONE behavior change) → transports introduced with both functions still alive (the pure-move commit, `git diff --color-moved` as the review artifact) → the merge (delete the remote function) → micro-duplication cleanup. The map verified: outside three transport windows (the two per-file middles + the remote transfer phase), the two bodies are code-identical — the residual diff is comments, two string literals, one kwarg, and one closure.

**Tech Stack:** two plain classes (no Protocol — nothing runs static checks; closures can't carry the attributes legibly); the `_duplicate_gate` verdict idiom for `enqueue`.

---

## Context for a zero-context engineer

- Repo root: `/Users/julius/conductor/workspaces/vireo/nagoya`; branch `import-unify-pr7-transport-merge` (tracks origin/main at the PR-6b merge `486d85df`). Baseline: `python -m pytest vireo/tests/test_import_job.py -q` → **231 passed, 1 skipped**. Re-grep every line ref before editing.
- `vireo/import_job.py` (4321 lines): remote `_run_remote_import_job` def 2985 (body 3005-3787); local `run_import_job` def 3790 (body 3797-4321, entry+dispatch at 3803-3811).
- **What remains transport-divergent (everything else is shared calls or byte-identical):**
  - Remote-only: `rt/remote/rsync_bin/ssh_base` preamble (3011-3014) + `import move as move_mod` (3025) → move verbatim into `_RsyncTransport.__init__`; `_emit_transfer` (3039-3084) → transport-internal; `ssh_dest` compute (3172-3175, used only by the transfer phase); the per-file transport middle (3216-3396: stat → eager hash → intra-batch dedup vs `queued_src_hashes` → collision/adopt walk vs `claimed_basenames`+mount → reserve+`to_transfer.append`); `_drop_queued_transfers` (3449-3460); the transfer phase (3467-3689).
  - Local-only: the per-file `same_file`/dest-under-source guard (3985-4028 — decision 12, see below); the per-file middle (4029-4186: stat → `_src_hash_cached` lazy closure → collision/adopt walk → adopt landing OR `copy_and_hash_verify` + booking + landing).
  - Parameter deltas: `verified_counted_for_copies` (`True` local 3928 / `params.verify_by_hash` remote 3156), `dest_noun`, `log_label`, `remote_unverified=not params.verify_by_hash` at the remote finalize — ALL collapse to transport attributes; `remote_unverified=not transport.attests_bytes` is exactly equivalent.
- **Three deliberate adaptations of the spec's protocol sketch (Task 1 records them with rationale):**
  1. **Verdict-style `enqueue`, not pure `_FileOutcome | Queued` / `_BatchResult`.** The real per-file middle has FIVE terminal shapes (landed / failed / intra-batch dup-skip with `dup_skips` booking / adopted landing / dest-read-cancelled breaking the batch), and `_record_checker` needs the RAW `src_hash` (un-normalized — `None` for remote zero-byte), not `_LandedFile.verified_hash` (normalized to EMPTY in 6b). Threading that through value types is a redesign, not a move. Mirror the proven `_duplicate_gate` idiom: mutate `state`/`batch_st`, return `_ENQ_HANDLED` / `_ENQ_QUEUED` / `_ENQ_CANCELLED`. The pure `_BatchResult` form is recorded as a PR 7b option (needs a `record_hash` field beside `verified_hash` + a zero-byte `run_verified_hashes` asymmetry pin first).
  2. **Two collision/adopt walks retained, one inside each transport.** They still differ on seven axes (loop shape; reservation map; local's size pre-check before hashing candidates — an I/O difference; local's explicit zero-byte adopt branch which remote can only reach checker-less — a live per-transport behavior split; eager vs lazy hashing; DestReadCancelled propagation; output shape). Unifying them = at least three behavior flips hidden inside "the merge PR" with no red-green of their own. The spec's non-goal is explicit: the seam must make a third transport POSSIBLE, not unify everything. Walk unification = PR 7b, each flip red-green'd.
  3. **`defers_transfer` dropped.** It existed only to parameterize a shared walk's hashing strategy; with two walks it has no reader.
  Also record: **the monkeypatch seam is NOT repointed** — all 29 `monkeypatch.setattr(_move, ...)` sites (15 `_run_rsync_streamed`, 9 `_remote_mkdir_p`, 5 `remote_verify_files`; 64 uses of the shared installer) keep working because `_RsyncTransport` keeps `import move as move_mod` and calls `move_mod.<name>(...)` at call time. CONSTRAINT: keep `_run_rsync_streamed`'s call shape BYTE-IDENTICAL (four tests wrap the installed fake and re-call with the same layout — test lines 1955, 7797, 7883, 8058) and `remote_verify_files`'s `(rsync_bin, src_specs, rsync_target, remote, dest_is_dir=…)` shape.
- **Decision 12 (new spec row, Task 4):** the local per-file guard (3985-4028) is NOT subsumed by `_batch_preflight`'s folder-level guard — it additionally catches `os.path.samefile` identity (hard-links/symlinks) and a `dest_file` that is itself a symlink into the card (realpath'd at file level). On remote, that geometry is exactly the safe_to_format-over-unbacked-bytes hazard the batch guard's own comment describes; today remote has no per-file defense. **Port to both paths (orchestrator-level, before `transport.enqueue`)** — a pure tightening (can only convert an accept into a failure in an already-unsafe geometry; expected existing-suite flips: zero). Red-green: remote test with `dest_file` symlinked into the card, `verify_by_hash=True` → `failed == 1`, `safe_to_format is False`.
- **The transfer phase ↔ `flush_batch`:** lines 3467-3689 move verbatim into `_RsyncTransport.flush_batch(state, batch_st, *, rel, checker)`. Its guard `if batch_st.to_transfer and not state.cancelled:` comes along, so the orchestrator calls `transport.flush_batch(...)` unconditionally (a no-op for `_LocalTransport` — `to_transfer` is always empty there anyway). It uses TWO different cancel probes on purpose (the watchdog's non-blocking `cancellation_requested` vs the loop-boundary pausing `is_cancelled`) — preserve both; do not collapse to one `stop` param. The outcome-completeness invariant already holds in the code (flat cancel → zero outcomes; renamed cancel → keeps prior successes).
- **Dead code to delete (found by the map):** `_path_under_destination` assigned-never-used in BOTH bodies (3022, 3821); `_path_under_any_source` assigned-never-used in the REMOTE body (3021). Local's per-file guard DOES use `_path_under_any_source` (via ctx) — keep local's binding until Task 5 restructures.
- **Micro-duplications to collapse in Task 7 (not before — they sit inside the code Tasks 5-6 move):** the copy-booking triple (remote 3666-3669 / local 4173-4175 — NOTE the order differs but effect is identical; unify as `_book_copied(state, rel, attests_bytes)`), the adoption booking (remote 3328-3371 / local 4131-4146 → `_book_adoption(...)`), the `_LandedFile(origin="copied")` constructions, and the ~130 lines of duplicated run-scope ledger narration comments (merge into one block near the orchestrator).
- **Stale references to fix in Task 6:** module docstring/comment mentions of `_run_remote_import_job` at 1948, 2704, 2742; test-file comment/docstring mentions at 8906, 9549, 10836. Direct callers: production exactly one (the dispatch, 3811); tests ZERO (verified).
- Contract pins throughout: `run_import_job(job, runner, db_path, workspace_id, params)` signature; the 19-key result dict; phase strings; `FakeRunner` surface. `app.py:26592` is the only production caller.

### Task 0: Sanity
- [ ] Branch check; baseline 231 passed, 1 skipped.

### Task 1: Spec amendments (docs-only)
- [ ] In the spec: (i) a dated "PR 7 as-built adaptations" note under the Transport protocol section recording the three adaptations above WITH their rationale (five terminal shapes + raw-src_hash; the seven-axis walk divergence table condensed; defers_transfer has no reader; ALSO the flush_batch deviations — it mutates state rather than returning _BatchResult, internalizes emit_transfer, and deliberately keeps TWO cancel probes (the watchdog's non-blocking cancellation_requested vs the loop-boundary pausing is_cancelled) instead of the sketch's single stop param) and the PR 7b deferral (walk unification + optional pure-_BatchResult form + `record_hash`); (ii) decision-table row 12 (the per-file guard port: what it catches beyond the folder guard, why porting is a tightening, red-green in this PR); (iii) the "do not repoint the monkeypatch seam" note with the call-shape constraints. Commit: `"Spec: PR 7 as-built adaptations, decision 12, seam-preservation constraints"`

### Task 2: Zero-flip hoists
- [ ] Hoist `_drop_queued_transfers` to module level as `_drop_queued_transfers(state, batch_st, rel)` (body from 3449-3460; update its B023 comment (now obsolete — module-level explicit params replace the default-arg binding it described) and keep the reason string byte-identical); call `_rollback_on_mount_loss(..., extra_rollback=...)` unconditionally on BOTH paths with a lambda or partial binding the current batch state (no-op locally — `to_transfer` is always empty; say so in a comment). Delete the three dead ctx locals (3021, 3022, 3821 — keep local's `_path_under_any_source`). Full file → 231/1. Commit: `"Hoist the queued-transfer rollback; drop dead ctx bindings"`

### Task 3: Rename `verified_counted_for_copies` → `attests_bytes`
- [ ] Both assignment sites (3156, 3928), all seven read sites (rollback calls 3463/4225, scan-prescan calls 3712/4247, stamping calls 3744/4272, remote booking 3668), and the PARAMETER NAMES in `_rollback_on_mount_loss`/`_catalog_scan_and_prescan`/`_reclassify_landed_failed` (grep for the old name — zero hits after). Update the two flag comments (the "PR 7's transport absorbs this flag" forward pointers are now discharged — say the transport attribute lands in Task 5). Also switch the remote finalize to `remote_unverified=not attests_bytes` (identical value). Pure rename; full file → 231/1. Commit: `"Rename verified_counted_for_copies to attests_bytes (spec PR 7)"`

### Task 4: Decision 12 — port the per-file guard (red-green)
- [ ] Test first: `test_remote_import_rejects_dest_file_symlinked_into_source` — geometry: remote import where the template `dest_file` path on the mount is a SYMLINK pointing back into the card (create the day-dir, `os.symlink(card_file, mount_day/name)`), `verify_by_hash=True`, `skip_duplicates=False`. Today the remote collision walk hashes the symlink target (= the card file), byte-matches, adopts → `skipped_duplicate == 1`, `safe_to_format` can go True over unbacked bytes. Assert the NEW behavior: `failed == 1`, `skipped_duplicate == 0`, `safe_to_format is False`, reason contains "resolves inside a source directory". RED today. Local pins exist for the nested/self-copy geometries (test lines ~3164, ~3298) but NOT for the symlinked-dest_file geometry — add the local symlink twin anyway (expected GREEN; makes decision 12's red-green symmetric across transports for one cheap test).
- [ ] Implement: move the guard block (3985-4028) into a module-level `_reject_source_backed_dest(state, ctx, *, rel, source_file, dest_file) -> bool` (True = rejected, caller `continue`s); call it in BOTH per-file middles immediately after the dest path is known (local: where it sits today; remote: right after the primary-name dest path is formed, BEFORE the walk so the walk never hashes a source-backed primary path). HONESTY CONSTRAINT for the guard comment AND the decision-12 spec row: a source-backed SUFFIX candidate (e.g. a symlink at `name_1.ext` into the card, different bytes at the primary) is caught by NEITHER guard on EITHER path — that per-candidate gap is pre-existing on local and this port deliberately preserves parity rather than closing it (closing it is its own behavior change; record as a PR 7b/follow-up item). The comment must say exactly that — never claim suffix coverage that doesn't exist. Full file → 231+1(or 2) passed; the new remote test flips red→green; nothing else moves. Commit: `"Both paths reject a per-file destination resolving into a source (spec decision 12)"`
- [ ] Preflight-mirror check: this guard is import-job-side only; the app.py preflight mirrors the collision walk, not this guard — n/a, state in the PR body.

### Task 5: Introduce the transports (the pure-move commit)
- [ ] Add `_ENQ_HANDLED/_ENQ_QUEUED/_ENQ_CANCELLED` module constants (comment: the caller maps CANCELLED→break — same contract as `_GATE_CANCELLED`, PR #1423) and the two classes:

```python
class _LocalTransport:
    attests_bytes = True
    dest_noun = "archive"
    log_label = "Import"
    def __init__(self, params, ctx): ...
    def enqueue(self, state, batch_st, *, source_file, rel, checker,
                stop_requested): ...   # the local middle 4029-4186, verbatim
    def flush_batch(self, state, batch_st, *, rel, checker):
        return  # copies land in enqueue; nothing deferred

class _RsyncTransport:
    dest_noun = "mount"
    log_label = "Remote import"
    def __init__(self, job, runner, params, ctx):
        # rt/remote/rsync_bin/ssh_base preamble + import move as move_mod
        # + the _emit_transfer construction (needs job/runner/state? -> read
        #   _emit_transfer's closures: job, runner, state.folder_counts; pass
        #   state per-call instead of capturing — adjust minimally and note it)
    @property
    def attests_bytes(self): return self._params.verify_by_hash
    def enqueue(...): ...              # remote middle 3216-3396 verbatim
    def flush_batch(self, state, batch_st, *, rel, checker): ...
        # ssh_dest compute + transfer phase 3467-3689 verbatim (incl. its guard)
```

  Each function constructs ITS OWN transport and calls it — both functions still exist and pass their suites. NOTE for the pure-move reviewer: after the Task-4 guard hoist the orchestrator computes the primary dest path for the guard and local `enqueue` recomputes the identical value internally (verbatim moved line) — deliberate duplication to keep the move pure; collapse in Task 7 if trivial. `_src_hash_cached` moves inside `_LocalTransport.enqueue` (it's per-file). The per-file loop in each function becomes: identical head + `_reject_source_backed_dest` + `verdict = transport.enqueue(...)` + the three-line mapping. `flush_batch` called unconditionally after the rollback block. KEEP every moved line verbatim — comments included; the review artifact is `git diff -M --color-moved=dimmed-zebra` showing moves, not edits. Full file → same count as Task 4's exit. Commit: `"Introduce _LocalTransport/_RsyncTransport; both paths run through the seam (pure move)"`

### Task 6: The merge
- [ ] Delete `_run_remote_import_job` entirely; `run_import_job` keeps its exact signature, constructs `db`, sets the workspace, then `transport = _RsyncTransport(job, runner, params, ctx) if params.remote_target is not None else _LocalTransport(params, ctx)` and runs the single orchestrator body (state uses `transport.log_label`; `dest_noun=transport.dest_noun`; stamping `attests_bytes=transport.attests_bytes`; finalize `remote_unverified=not transport.attests_bytes` — degenerates to False locally, exactly today's default). CAREFUL with ordering: ctx is built before the transport (the transport takes it). Fix the six stale `_run_remote_import_job` references (production 1948/2704/2742; test comments 8906/9549/10836). PRESERVE the deleted remote function's docstring content (the verification honesty contract: hash_status only on the checksum path; safe_to_format=False with the enable-verify_by_hash reason) — it moves into `_RsyncTransport`'s class docstring. Full file → same count; then BY NAME: the parity suite, the four fake-wrapping tests (1955/7797/7883/8058), the cancellation trio (7736/8007/8073), the `remote_verify_files` failure tests (5904/7414), the mount-detach pins, the zero-byte quartet + 6b guard pair. Commit: `"One import orchestrator: delete _run_remote_import_job (the transport merge)"`
- [ ] Also update the module docstring and CLAUDE.md if either describes the two-function structure (grep "two" + "path" in both).

### Task 7: Cleanup
- [ ] Collapse the micro-duplications now living across the transports: `_book_copied(state, rel, attests_bytes)` (NOTE the two sites' statement ORDER differs but the effect is identical — pick one order, say so), `_book_adoption(state, batch_st, rel, ...)`, and the duplicated `_LandedFile(origin="copied")` constructions where signatures allow; merge the ~130 lines of duplicated run-scope ledger narration into one comment block at the orchestrator. No behavior; full file → same count. Commit: `"Collapse post-merge micro-duplications"`

### Task 8: Verification + PR
- [ ] Full file + required CLAUDE.md suite (known env failure ignored). Function/file size accounting for the PR body (expect the file to shrink by roughly the remote function's skeleton — several hundred lines).
- [ ] Push; PR: title `"Import unification PR 7: one orchestrator behind a transport seam — delete _run_remote_import_job"`, base main. Body: spec/plan links; the three sketch adaptations with rationale + PR 7b deferral; decision 12 with its red-green; the seam-preservation note (zero monkeypatch churn, the four wrapping tests and call-shape constraint); the pure-move review artifact for Task 5; what this closes (every import fix now lands once — the project's original goal); remaining optional work (PR 7b walk unification, PR 8 preflight de-mirror); exact counts. End with the Claude Code attribution line.
